[project]
name = "alarm-button-api-service"
version = "0.1.0"
description = "FastAPI SSE service for alarm button"
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn>=0.30.0",
]

[tool.uv]
package = false

